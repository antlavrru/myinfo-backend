{
  "rewrites": [
    {
      "source": "/submit-review",
      "destination": "/server.js"
    },
    {
      "source": "/(.*)",
      "destination": "/server.js"
    }
  ]
  // Временно уберем блок "headers", чтобы исключить его влияние на 405.
  // Если это решит 405, то CORS ошибка (403) появится, и это будет прогрессом.
}